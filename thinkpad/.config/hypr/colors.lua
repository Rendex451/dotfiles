-- ~/.config/hypr/colors.lua
-- Converted from colors.conf ($border_active / $border_inactive).
-- Required from hyprland.lua via: local colors = require("colors")

return {
    border_active   = "rgb(fe8019)",
    border_inactive = "rgb(504945)",
}
